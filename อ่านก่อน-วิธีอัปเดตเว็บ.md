# Trip Share ชิงเต่า — เวอร์ชัน 2 (หารค่าใช้จ่าย + AI อ่านบิล + โหวตเปลี่ยนแพลน)

ใช้ Firebase โปรเจกต์เดิม (`trip-share-aead6`) รายชื่อที่ลงไว้แล้วยังอยู่ครบ

## มีอะไรใหม่

| แท็บ | สิ่งที่ทำได้ |
|---|---|
| **แพลน & โหวต** | เสนอเพิ่มหรือตัดที่เที่ยว ย้ายไปอีกวัน สลับทั้งวัน เลื่อนวันเดินทาง หรือเสนอเรื่องอื่น ทุกคนโหวตเห็นด้วย/ไม่เห็นด้วยได้คนละ 1 เสียง ข้อเสนอเรียงตามคะแนน (มากสุดอยู่บน) ถ้าได้เสียงเห็นด้วย **เกินครึ่งของคนที่ "ไปแน่นอน"** แพลนจะเปลี่ยนให้ทันที และกดย้อนกลับได้ |
| **หารค่าใช้จ่าย** | ถ่ายรูปหรือเลือกรูป/PDF ของบิล ใบเสร็จ สลิป หรือตั๋ว แล้ว AI จะอ่านร้าน วันที่ ยอดเงิน สกุลเงิน (บาท/หยวน) และรายการในบิล หารได้ 3 แบบ: เท่ากัน, กำหนดเองรายคน, หารตามรายการที่แต่ละคนสั่ง (ค่าบริการ/VAT จะเฉลี่ยตามสัดส่วนให้เอง) มีสรุปรายคน บอกว่าใครต้องโอนให้ใครเท่าไร กด "บันทึกว่าโอนแล้ว" ได้ ถ้าส่งสลิปโอนคืนให้ AI อ่าน ระบบจะบันทึกเป็นการโอนคืนให้เอง คัดลอกสรุปไปส่ง LINE และดาวน์โหลด CSV เปิดใน Excel ได้ |
| **ประมาณการ** | ตัวคำนวณค่าใช้จ่ายต่อคนตามจำนวนคนเหมือนเดิม (อัปเดตเรตเป็น 1 หยวน ≈ 4.95 บาท) |

## ขั้นตอน (ทำครั้งเดียว ประมาณ 10 นาที)

### 1. เปิดใช้ AI (ฟรี ไม่ต้องผูกบัตร)
1. เปิด https://console.firebase.google.com/project/trip-share-aead6/ailogic
2. กด **Get started** แล้วเลือก **Gemini Developer API**
3. รอประมาณ 1–2 นาทีให้ระบบเปิดใช้งาน

> ถ้ายังไม่เปิด เว็บยังใช้ได้ปกติ แต่ปุ่มให้ AI อ่านบิลจะขึ้นข้อความว่ายังไม่ได้เปิดใช้ และต้องกรอกยอดเอง

### 2. อัปโหลดเว็บและ Firestore rules
1. ติดตั้ง **Node.js LTS** จาก https://nodejs.org (ถ้ายังไม่มี)
2. ดับเบิลคลิก **`deploy.bat`** ในโฟลเดอร์นี้
3. หน้าเว็บจะเปิดให้ล็อกอิน Google ใช้บัญชีที่เป็นเจ้าของโปรเจกต์ Firebase นี้
4. รอจนขึ้น `Deploy complete!` แล้วเปิด https://trip-share-aead6.web.app

> **ต้อง deploy rules ด้วย** ตอนนี้ rules เดิมเปิดให้เขียนได้แค่รายชื่อ ถ้าไม่อัปเดต ค่าใช้จ่ายกับโหวตจะบันทึกไม่ได้ (ขึ้นข้อความ `permission-denied`)
> ถ้าอยากอัปเดตเฉพาะ rules โดยไม่ใช้ `deploy.bat` ให้ไปที่ Firebase console → Firestore Database → Rules แล้ววางเนื้อหาไฟล์ `firestore.rules` จากนั้นกด Publish

### 3. App Check (ตั้งค่าแล้ว เหลือกด Enforce)
ตั้งแต่ **2 พ.ย. 2026** Firebase จะบล็อกการเรียก AI ที่ไม่มี App Check
- ลงทะเบียน reCAPTCHA Enterprise แล้ว และใส่ site key ไว้ใน `public/index.html` แล้ว (`const APP_CHECK = ...`)
- สิ่งที่เหลือ: Firebase console → **App Check** → แท็บ **APIs** → **Firebase AI Logic** → **Enforce** (ไม่ต้อง enforce Cloud Firestore)

## เก็บข้อมูลไว้ที่ไหน (Firestore)
`trips/qingdao/` → `signups` (รายชื่อ เหมือนเดิม), `expenses` (ค่าใช้จ่ายและการโอนคืน), `receipts` (รูปบิลย่อขนาด), `proposals` (ข้อเสนอ + โหวต), `meta/plan` (แพลนปัจจุบัน)

รูปบิลถูกย่อและเก็บใน Firestore เลย ไม่ใช้ Cloud Storage จึงไม่ต้องอัปเกรดเป็น Blaze plan

## ข้อควรรู้
- ไม่ต้องสมัครสมาชิก ทุกเครื่องได้ตัวตนอัตโนมัติ **แก้ไข/ลบได้เฉพาะรายการที่บันทึกจากเครื่องนั้น**
- ถ้าลงชื่อไว้จากมือถือ แล้วมาใช้คอม ให้เลือก "คุณคือ" เป็นชื่อตัวเองได้ ระบบนับโหวต **1 เสียงต่อ 1 คน** ถึงจะโหวตจากหลายเครื่องก็ตาม (นับเสียงล่าสุด)
- AI อาจอ่านผิดได้ ระบบจึงกรอกให้ก่อน แล้วให้คนตรวจและกดบันทึกเอง
- ค่าเงินหยวนแปลงเป็นบาทตามเรตที่ใส่ในแต่ละรายการ (ค่าเริ่มต้น 4.95 แก้ได้ และเครื่องจะจำเรตล่าสุดไว้)
- ถ้าจะเปลี่ยนรุ่น AI ให้แก้ `AI_MODELS` ใน `index.html` (ตอนนี้ใช้ `gemini-3.8-flash` และสลับไป `gemini-3.5-flash` อัตโนมัติถ้าตัวหลักช้าเกิน 30 วินาทีหรือล่ม)

## โครงไฟล์
```
public/index.html      หน้าเว็บทั้งหมด
public/logic.js        สูตรหารเงิน เคลียร์ยอด โหวต และปรับแพลน
firestore.rules        สิทธิ์การอ่านเขียนข้อมูล
firebase.json, .firebaserc, deploy.bat
test/logic.test.mjs    ทดสอบสูตร: node test/logic.test.mjs
```

## อัปเดตผ่าน GitHub (ไม่ต้องใช้ deploy.bat)
ไฟล์ `.github/workflows/deploy.yml` สั่งให้ GitHub ทดสอบสูตรแล้ว deploy เว็บ + Firestore rules ขึ้น Firebase ให้เองทุกครั้งที่มีการแก้ไฟล์ใน branch `main`
- ต้องมี secret ชื่อ `FIREBASE_SERVICE_ACCOUNT` ใน GitHub (Settings → Secrets and variables → Actions) เป็นเนื้อหาไฟล์กุญแจ JSON ของ service account `github-deploy` ทั้งไฟล์
- ดูผลได้ที่แท็บ **Actions** ของ repo: ✅ เขียว = ขึ้นเว็บแล้ว, ❌ แดง = กดเข้าไปดูว่าติดตรงไหน
- กด **Run workflow** ในแท็บ Actions เพื่อ deploy ซ้ำได้โดยไม่ต้องแก้ไฟล์
- **ห้ามอัปโหลดไฟล์กุญแจ JSON ขึ้น repo หรือส่งในแชต** ใส่ไว้ใน Secrets อย่างเดียว ถ้าหลุด ให้ลบกุญแจใน Google Cloud → IAM → Service accounts → github-deploy → Keys แล้วสร้างใหม่
